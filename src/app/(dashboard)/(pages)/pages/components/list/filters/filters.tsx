import ListFilters from '@/components/list/filters/filters';

import PagesListFilterStatus from './status';

export default function PagesListFilters() {
  return (
    <ListFilters>
      <PagesListFilterStatus />
    </ListFilters>
  );
}
